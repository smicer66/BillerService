package com.syncstate.probase.bills.BillerService.enums;

public enum ServiceProviderFieldType {
    STRING,
    INTEGER,
    DECIMAL,
    ENUMERATION,
    DATE,
    TIME,
    EMAIL,
    PHONE,
    FILE,
    JSON,
    PASSWORD
}
